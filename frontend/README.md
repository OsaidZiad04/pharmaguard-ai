# Frontend

Next.js dashboard for PharmaGuard AI.

## Run

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` in `.env.local` if the backend is not running at `http://localhost:8000`.

## Scope

This is a pharmacist review dashboard scaffold. It uses mock backend endpoints and draft-only wording.
